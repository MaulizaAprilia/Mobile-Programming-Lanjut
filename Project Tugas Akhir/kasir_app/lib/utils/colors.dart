import 'package:flutter/material.dart';

/// Warna utama aplikasi (utama untuk AppBar, Button, dll)
const Color primaryColor = Color(0xFF008080); // Teal

/// Warna aksen untuk tombol atau ikon penting
const Color accentColor = Color(0xFFFFA726); // Orange

/// Warna latar belakang umum
const Color backgroundColor = Color(0xFFF5F5F5); // Light grey

/// Warna untuk teks gelap
const Color textColor = Color(0xFF333333);

/// Warna untuk border atau garis pemisah
const Color borderColor = Color(0xFFE0E0E0);

/// Warna sukses atau positif (misal pembayaran berhasil)
const Color successColor = Color(0xFF66BB6A); // Green

/// Warna error (misal validasi gagal)
const Color errorColor = Color(0xFFEF5350); // Red
